# Copy this file to .env
# Local dev mode: only these two matter right now. Everything else (Postgres,
# Redis, Celery settings) is commented out in settings.py until later phases.

DEBUG=True
SECRET_KEY=change-this-to-a-long-random-string
ALLOWED_HOSTS=localhost,127.0.0.1

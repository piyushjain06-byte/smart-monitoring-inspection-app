Django==5.0.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-environ==0.11.2
django-cors-headers==4.4.0
django-simple-history==3.7.0
Pillow==10.4.0
WeasyPrint==59.0
boto3==1.28.0
django-storages==1.14.1
opencv-python-headless==4.10.0.84

# --- Phase 9 (Part 23) — AI anomaly detection / risk engine ---
scikit-learn==1.5.2
numpy==2.1.3

# --- Phase 4.5 / 4.7 — Channels + Celery (needs Redis) ---
# Redis itself is NOT a pip package you "run" — you need a Redis server
# reachable at REDIS_URL (see .env.example). Locally, either install Redis
# directly, or just run the `redis` service from docker-later/docker-compose.yml
# on its own:  docker compose -f docker-later/docker-compose.yml up redis
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
celery==5.4.0
redis==5.0.8

# --- Added back in later phases, once we're ready for them ---
# psycopg2-binary       # Phase: switching from SQLite to Postgres+PostGIS
# djangorestframework-gis
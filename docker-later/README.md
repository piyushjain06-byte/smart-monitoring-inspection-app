These files (Dockerfile, docker-compose.yml) are for later — once you've got
Docker Desktop's virtualization issue sorted (or decide to set it up on a
different machine). They match the original Postgres+PostGIS+Redis+Celery
stack from the full implementation plan.

For now, ignore this folder — the project runs with plain Python + SQLite,
see the main README.md in the project root.
